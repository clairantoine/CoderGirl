## Notes- 3.20.19 ##


#mario exercise#

public static void main (String[] args)
Sstemoutprintln ("How many levels?");
scanner keyboard = new Scanner (Sytem.in);

int levels= keyboard.nextInt);
keyboard .skip ("\n");

	for(int i = 1; i <= lvels; i++)
	{
		System.out.print(" ");  [levels minus 1 is how many spaces you need]
	}
	for(int k= 0; k<i +1; k++)
	{
		System.out.print("#");
	}
	System.out.print("\n");
	}
}
}


#Review: Array#

int[] array = {1,2,3};
-OR-
= new int[3]


*used for storing data*
ARRAYS CANNOT CHANGE SIZE. If you have 6 digits in an array, it stays at 6.
a LIST however, can be resized

## Binary ##
everything in binary is a boolean. "Yes" or "No".

ASCII- character set
Hexadecimal - to Base 16 (rather than base 2)



## pair program exercise ##

import java.util.Scanner;

public class ArrayNumbers 
{
public static void main (String args[])
{
Scanner s = new Scanner (System.in);
int[] numbers = new int [5];

for(int index = 0; index < number.length; index++)
{
System.out.println("Give me a number: ");
number[index] = s.nextInt();

}
int max = numbers[0];
for (int index = 1; turd< numbers.length; turd++)
{
	if(numbers[turd]> max)
	{
	max = numbers[turd];
	}
}
System.out.println("The Largest number is " +max);
}
}


## hidden number exercise ##

radix: 2 -> sets the number to BASE TWO
regex: "" -> part of the "split"